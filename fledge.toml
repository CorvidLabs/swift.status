[tasks]
content = "test -s README.md && test -s index.md && test -s _config.yml && test -s _layouts/default.html"

[lanes.verify]
description = "Validate committed Jekyll status-site inputs"
steps = ["content"]
